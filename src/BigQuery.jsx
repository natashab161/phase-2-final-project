import React from 'react';

function BigQuery() {
  return (
    <div>
      <button onClick={uploadImageMetadata}>Upload Image Metadata to BigQuery</button>
    </div>
  );
}

export default BigQuery;
